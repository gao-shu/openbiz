<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.openbiz.access.mapper.OpenbizAccessRecordMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO openbiz_access_record (tenant_id, user_id, device_id, command_id, result, create_time)
        VALUES (#{tenantId}, #{userId}, #{deviceId}, #{commandId}, #{result}, #{createTime})
    </insert>

</mapper>
