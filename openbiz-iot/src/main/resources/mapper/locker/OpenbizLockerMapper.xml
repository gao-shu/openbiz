<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.openbiz.locker.mapper.OpenbizLockerMapper">

    <resultMap id="LockerResult" type="com.openbiz.locker.domain.OpenbizLocker">
        <id     property="id"         column="id"/>
        <result property="tenantId"   column="tenant_id"/>
        <result property="deviceId"   column="device_id"/>
        <result property="name"       column="name"/>
        <result property="status"     column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectByIdAndTenant" resultMap="LockerResult">
        SELECT id, tenant_id, device_id, name, status, create_time, update_time
        FROM openbiz_locker
        WHERE id = #{id} AND tenant_id = #{tenantId}
        LIMIT 1
    </select>

</mapper>
