<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.openbiz.locker.mapper.OpenbizLockerSlotMapper">

    <resultMap id="SlotResult" type="com.openbiz.locker.domain.OpenbizLockerSlot">
        <id     property="id"         column="id"/>
        <result property="tenantId"   column="tenant_id"/>
        <result property="lockerId"   column="locker_id"/>
        <result property="slotNo"     column="slot_no"/>
        <result property="status"     column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectByIdAndTenant" resultMap="SlotResult">
        SELECT id, tenant_id, locker_id, slot_no, status, create_time, update_time
        FROM openbiz_locker_slot
        WHERE id = #{id} AND tenant_id = #{tenantId}
        LIMIT 1
    </select>

    <update id="updateStatus">
        UPDATE openbiz_locker_slot
        SET status = #{status}, update_time = NOW()
        WHERE id = #{id} AND tenant_id = #{tenantId}
    </update>

</mapper>
