<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.openbiz.mes.mapper.OpenbizMesProductionRecordMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO openbiz_mes_production_record
          (tenant_id, work_order_id, device_id, quantity, result, command_id, create_time)
        VALUES
          (#{tenantId}, #{workOrderId}, #{deviceId}, #{quantity}, #{result}, #{commandId}, #{createTime})
    </insert>

</mapper>
