# openbiz-shop

**Shop** domain �� Phase 1: Product + Inventory CAS + SUCCESS Order.

```text
placeOrder = validate + CAS deduct + insert SUCCESS order (one TX)
```

Depends on `openbiz-saas-core` only among OpenBiz domains.
Probe: `/openbiz/test/shop/**`
