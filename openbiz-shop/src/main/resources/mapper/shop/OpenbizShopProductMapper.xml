<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.openbiz.shop.mapper.OpenbizShopProductMapper">
    <resultMap id="P" type="com.openbiz.shop.domain.OpenbizShopProduct">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="productCode" column="product_code"/>
        <result property="productName" column="product_name"/>
        <result property="price" column="price"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO openbiz_shop_product
        (tenant_id, product_code, product_name, price, status, create_time, update_time)
        VALUES (#{tenantId}, #{productCode}, #{productName}, #{price}, #{status}, #{createTime}, #{updateTime})
    </insert>
    <select id="selectByIdAndTenant" resultMap="P">
        SELECT id, tenant_id, product_code, product_name, price, status, create_time, update_time
        FROM openbiz_shop_product WHERE id = #{id} AND tenant_id = #{tenantId} LIMIT 1
    </select>
    <select id="selectByCodeAndTenant" resultMap="P">
        SELECT id, tenant_id, product_code, product_name, price, status, create_time, update_time
        FROM openbiz_shop_product WHERE product_code = #{productCode} AND tenant_id = #{tenantId} LIMIT 1
    </select>
</mapper>
